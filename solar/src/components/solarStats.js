// Figures about UK solar that aren't in the feed. Kept together and named so
// that the header and the FAQ quote one source rather than two hardcoded
// strings that can drift apart.

// Hard coded for now, until we find a good energy demand API.
export const DEMAND_SHARE_LABEL = "4.5%";
